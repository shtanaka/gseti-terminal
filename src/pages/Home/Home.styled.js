import styled from "styled-components";
import { DEFAULT_FONT_SIZE, DEFAULT_LINE_HEIGHT } from "shared/styleguide";

export default styled.div`
  padding: 10px;
  font-size: ${DEFAULT_FONT_SIZE};
  line-height: ${DEFAULT_LINE_HEIGHT};
  color: limegreen;
`;